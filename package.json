{
  "name": "dev-blog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "client": "cd client && npm run dev",
    "server": "cd server && npx tsx index.js",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "npm run install-all && cd client && npm run build && cd ../server && npx prisma generate",
    "start": "cd server && NODE_ENV=production npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
